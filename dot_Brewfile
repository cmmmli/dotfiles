# ~/.Brewfile
#
# 方針:
#   - Homebrew: cask / コンパイル物 / 「常に最新でよい」汎用 CLI
#   - mise (~/.config/mise/config.toml): 言語ランタイムと、バージョンを固定したい CLI
#     (node, python, go, terraform, kubectl, helm, ...)
#   - 依存ライブラリ (libgit2, glib, folly など) は brew が自動で入れるので列挙しない

# ---------------------------------------------------------------------------
# taps
# ---------------------------------------------------------------------------
tap "k1low/tap"        # git-wt
tap "microsoft/apm"    # apm
tap "ngrok/ngrok"      # ngrok (cask)

# ---------------------------------------------------------------------------
# shell / prompt / plugin
# ---------------------------------------------------------------------------
brew "zsh"
brew "starship"
brew "sheldon"

# ---------------------------------------------------------------------------
# git / GitHub
# ---------------------------------------------------------------------------
brew "git"
brew "gh"
brew "ghq"
brew "lazygit"
brew "k1low/tap/git-wt"

# ---------------------------------------------------------------------------
# editor
# ---------------------------------------------------------------------------
brew "vim"
brew "neovim"

# ---------------------------------------------------------------------------
# CLI utilities
# ---------------------------------------------------------------------------
brew "bat"
brew "colordiff"
brew "eza"
brew "fd"
brew "htop"
brew "jq"
brew "peco"
brew "shellcheck"
brew "tree"
brew "watch"
brew "watchman"
brew "wget"
brew "yq"

# ---------------------------------------------------------------------------
# media
# ---------------------------------------------------------------------------
brew "ffmpeg"

# ---------------------------------------------------------------------------
# language / DB / misc (コンパイル物や汎用ツール)
# ---------------------------------------------------------------------------
brew "perl"
brew "postgresql@15"
brew "uv"
brew "biome"

# ---------------------------------------------------------------------------
# container (colima = VM + Docker Engine, docker = CLI, compose/buildx = CLI プラグイン)
#   プラグインは ~/.docker/cli-plugins/ のシンボリックリンク (chezmoi 管理) で docker から見える
# ---------------------------------------------------------------------------
brew "colima"
brew "docker"
brew "docker-compose"
brew "docker-buildx"

# ---------------------------------------------------------------------------
# tool managers
# ---------------------------------------------------------------------------
brew "chezmoi"
brew "mise"   # ランタイム / pin したい CLI は ~/.config/mise/config.toml
brew "aqua"   # 仕事リポジトリの aqua.yaml 用 (グローバル設定は持たない)

# ---------------------------------------------------------------------------
# misc
# ---------------------------------------------------------------------------
brew "microsoft/apm/apm"

# ---------------------------------------------------------------------------
# casks
# ---------------------------------------------------------------------------
cask "1password-cli"
cask "codex"
cask "ghostty"
cask "linearmouse"
cask "ngrok"

# fonts
cask "font-hack-nerd-font"
cask "font-plemol-jp"
cask "font-plemol-jp-hs"
cask "font-plemol-jp-nf"
